pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven{
            url "https://naver.jfrog.io/artifactory/maven/"
        }
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "location_test"
include ':app'
